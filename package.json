{
  "name": "excel-to-json-data",
  "version": "1.0.0",
  "description": "Excel parser to get data tracked during the year and output it as json",
  "main": "data-parser.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sam Radford",
  "license": "ISC",
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
